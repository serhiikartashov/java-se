MyLoginConfig {
    com.sun.security.auth.module.KeyStoreLoginModule REQUISITE debug=true keyStoreURL="file:./src/main/resources/keystore.jks";
    // The key alias is 'mykey'
    // The keystore password is 'changeit'
    // The private key password is 'password'
};